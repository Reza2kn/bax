// Drizzle schema. Keep this file dialect-agnostic by using the `core` types
// where possible. Per-dialect tables (sqlite vs pg) live alongside if needed.
//
// For BAX v0 we don't have any tables yet — they'll arrive with BAX-3 (landing
// page) and BAX-4 (signup form). This file exists so `db:generate` works and
// future tables have an obvious home.

export {};
